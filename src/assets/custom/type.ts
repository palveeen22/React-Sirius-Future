export type TProps = {
    isActive: boolean
    className: string
}

export type TPropsNav = {
    Title: string,
    Icon: string,
    isACtive: boolean,
    Onclick?: any
}

export type TPropsSide = {
    active: boolean
}